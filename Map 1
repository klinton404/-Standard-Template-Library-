#include<bits/stdc++.h>
using namespace std;
int main()
{
    map<int,int>mp;
    mp[0]=10;
    mp.insert({1,20});
    mp.insert({2,30});
    mp.insert({3,40});
    mp.insert({4,50});

    mp[0]+=100;

    cout<<mp[0]<<endl;
    cout<<mp.at(1)<<endl;
}
